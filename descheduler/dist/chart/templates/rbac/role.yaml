{{- if .Values.rbac.enable }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: descheduler-manager-role
rules:
- apiGroups:
  - descheduler.cortex
  resources:
  - deschedulings
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - descheduler.cortex
  resources:
  - deschedulings/finalizers
  verbs:
  - update
- apiGroups:
  - descheduler.cortex
  resources:
  - deschedulings/status
  verbs:
  - get
  - patch
  - update
{{- end -}}
