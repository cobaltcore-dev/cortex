{{- if .Values.rbac.enable }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: {{ .Values.namePrefix }}-reservations-manager-role
rules:
- apiGroups:
  - reservations.cortex
  resources:
  - computereservations
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - reservations.cortex
  resources:
  - computereservations/finalizers
  verbs:
  - update
- apiGroups:
  - reservations.cortex
  resources:
  - computereservations/status
  verbs:
  - get
  - patch
  - update
{{- end -}}
