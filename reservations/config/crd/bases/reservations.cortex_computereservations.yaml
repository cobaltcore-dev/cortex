---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: computereservations.reservations.cortex
spec:
  group: reservations.cortex
  names:
    kind: ComputeReservation
    listKind: ComputeReservationList
    plural: computereservations
    shortNames:
    - cres
    singular: computereservation
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.host
      name: Host
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.error
      name: Error
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ComputeReservation is the Schema for the computereservations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ComputeReservation
            properties:
              bareResource:
                description: |-
                  If reservation kind is bare resource, this field will contain metadata
                  necessary to determine if the bare resource reservation can be fulfilled.
                properties:
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Resources requested to reserve.
                    type: object
                type: object
              domainID:
                description: The domain ID to reserve for.
                type: string
              instance:
                description: |-
                  If reservation kind is instance, this field will contain metadata
                  necessary to determine if the instance reservation can be fulfilled.
                properties:
                  extraSpecs:
                    additionalProperties:
                      type: string
                    description: Extra specifications for the instance.
                    type: object
                  flavor:
                    description: The flavor name of the instance to reserve.
                    type: string
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Resources requested to reserve for this instance.
                    type: object
                required:
                - flavor
                type: object
              kind:
                description: The kind of reservation.
                type: string
              projectID:
                description: The project ID to reserve for.
                type: string
            required:
            - domainID
            - kind
            - projectID
            type: object
          status:
            description: status defines the observed state of ComputeReservation
            properties:
              error:
                description: An error explaining why the reservation is failed, if
                  applicable.
                type: string
              host:
                description: The name of the compute host that was allocated.
                type: string
              phase:
                description: The current phase of the reservation.
                type: string
            required:
            - host
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
