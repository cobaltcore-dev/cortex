---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: schedulingdecisions.decisions.cortex
spec:
  group: decisions.cortex
  names:
    kind: SchedulingDecision
    listKind: SchedulingDecisionList
    plural: schedulingdecisions
    shortNames:
    - sdec
    - sdecs
    singular: schedulingdecision
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.error
      name: Error
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Created
      type: date
    - jsonPath: .status.description
      name: Description
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SchedulingDecision is the Schema for the schedulingdecisions
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SchedulingDecision
            properties:
              availbilityZone:
                type: string
              flavor:
                properties:
                  disk:
                    type: integer
                  name:
                    type: string
                  ram:
                    type: integer
                  vcpus:
                    type: integer
                required:
                - disk
                - name
                - ram
                - vcpus
                type: object
              input:
                additionalProperties:
                  type: number
                type: object
              live:
                type: boolean
              pipeline:
                properties:
                  name:
                    type: string
                  outputs:
                    items:
                      properties:
                        activations:
                          additionalProperties:
                            type: number
                          description: Weights calculated by this step subjected to
                            the activation function.
                          type: object
                        step:
                          type: string
                      required:
                      - step
                      type: object
                    type: array
                required:
                - name
                type: object
              resize:
                type: boolean
              vmware:
                type: boolean
            required:
            - flavor
            - pipeline
            type: object
          status:
            description: status defines the observed state of SchedulingDecision
            properties:
              deletedHosts:
                additionalProperties:
                  items:
                    type: string
                  type: array
                description: Hosts that were deleted during pipeline processing and
                  all steps that attempted to delete them.
                type: object
              description:
                type: string
              error:
                description: Only given if state is "error".
                type: string
              finalScores:
                additionalProperties:
                  type: number
                description: Final scores for each host after processing all pipeline
                  steps.
                type: object
              state:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
