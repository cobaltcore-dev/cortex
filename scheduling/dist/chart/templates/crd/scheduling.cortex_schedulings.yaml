{{- if .Values.crd.enable }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.crd.keep }}
    "helm.sh/resource-policy": keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.17.2
  name: schedulings.scheduling.cortex
spec:
  group: scheduling.cortex
  names:
    kind: Scheduling
    listKind: SchedulingList
    plural: schedulings
    singular: scheduling
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.operator
      name: Operator
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Created
      type: date
    - jsonPath: .status.took
      name: Took
      type: string
    - jsonPath: .status.error
      name: Error
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Scheduling is the Schema for the schedulings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Scheduling
            properties:
              operator:
                description: The operator by which this scheduling should be extracted.
                type: string
            type: object
          status:
            description: status defines the observed state of Scheduling
            properties:
              error:
                description: If there was an error during the last scheduling, it
                  is recorded here.
                type: string
              took:
                description: The time it took to schedule.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end -}}
